// Components/Layout/Card/BUICard.razor
@using CdCSharp.BlazorUI.Core.Abstractions.Behaviors.Design
@using CdCSharp.BlazorUI.Core.Abstractions.Behaviors.Javascript
@using CdCSharp.BlazorUI.Core.Css

@namespace CdCSharp.BlazorUI.Components.Layout

@inherits BUIVariantComponentBase<BUICard, BUICardVariant>
@implements IHasElevation
@implements IHasBorder
@implements IHasBackgroundColor
@implements IHasFullWidth
@implements IHasRipple

@{
    base.BuildRenderTree(__builder);
}

@code {
    private ElementReference _cardElement;

    // Content parameters
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public RenderFragment? Header { get; set; }
    [Parameter] public RenderFragment? Media { get; set; }
    [Parameter] public RenderFragment? Actions { get; set; }

    // IHasElevation
    [Parameter] public int? Elevation { get; set; } = 1;

    // IHasBorder
    [Parameter] public BorderStyle? Border { get; set; }
    [Parameter] public BorderStyle? BorderTop { get; set; }
    [Parameter] public BorderStyle? BorderRight { get; set; }
    [Parameter] public BorderStyle? BorderBottom { get; set; }
    [Parameter] public BorderStyle? BorderLeft { get; set; }

    // IHasBackgroundColor
    [Parameter] public CssColor? BackgroundColor { get; set; }

    // IHasFullWidth
    [Parameter] public bool FullWidth { get; set; }

    // IHasRipple
    [Parameter] public bool DisableRipple { get; set; }
    [Parameter] public CssColor? RippleColor { get; set; }
    [Parameter] public int? RippleDuration { get; set; }

    // Card specific
    [Parameter] public bool Clickable { get; set; }
    [Parameter] public EventCallback<MouseEventArgs> OnClick { get; set; }

    public override BUICardVariant DefaultVariant => BUICardVariant.Default;

    protected override IReadOnlyDictionary<BUICardVariant, Func<BUICard, RenderFragment>> BuiltInTemplates { get; } =
        new Dictionary<BUICardVariant, Func<BUICard, RenderFragment>>
        {
            [BUICardVariant.Default] = c => c.RenderDefault(),
            [BUICardVariant.Outlined] = c => c.RenderOutlined(),
            [BUICardVariant.Flat] = c => c.RenderFlat()
        };

    private RenderFragment RenderDefault() => __builder =>
    {
        <bui-component @attributes="ComputedAttributes">
            <div @ref="_cardElement"
                 class="bui-card @(Clickable ? "bui-card--clickable" : "")"
                 @onclick="HandleClick">
                @if (Media != null)
                {
                    <div class="bui-card__media">
                        @Media
                    </div>
                }
                @if (Header != null)
                {
                    <div class="bui-card__header">
                        @Header
                    </div>
                }
                @if (ChildContent != null)
                {
                    <div class="bui-card__content">
                        @ChildContent
                    </div>
                }
                @if (Actions != null)
                {
                    <div class="bui-card__actions">
                        @Actions
                    </div>
                }
            </div>
        </bui-component>
    };

    private RenderFragment RenderOutlined() => RenderDefault();
    private RenderFragment RenderFlat() => RenderDefault();

    private async Task HandleClick(MouseEventArgs args)
    {
        if (Clickable && OnClick.HasDelegate)
        {
            await OnClick.InvokeAsync(args);
        }
    }

    public ElementReference GetRippleContainer() => _cardElement;
}