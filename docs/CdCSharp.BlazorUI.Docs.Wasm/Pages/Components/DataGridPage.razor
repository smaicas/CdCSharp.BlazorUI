@page "/components/datagrid"

@inject IStringLocalizer<DataGridPage> Loc

<PageTitle>BlazorUI - BUIDataGrid</PageTitle>

<DocPage Eyebrow="@Loc["Component"]" Title="@Loc["BUIDataGrid"]"
         Lede="@Loc["Strongly-typed tabular data with sorting, filtering, pagination, selection, custom templates, and more — declaratively."]"
         PageInstance="this">

    <DocDemo Key="Basic" Title="@Loc["Basic usage"]" Description="@Loc["Bind Items and declare BUIDataColumn elements inside Columns."]">
        <BUIDataGrid Items="_people">
            <Columns>
                <BUIDataColumn TItem="Person" Property="@(p => p.Id)" Header="ID" />
                <BUIDataColumn TItem="Person" Property="@(p => p.Name)" Header="@Loc["Name"]" />
                <BUIDataColumn TItem="Person" Property="@(p => p.Email)" Header="@Loc["Email"]" />
                <BUIDataColumn TItem="Person" Property="@(p => p.Age)" Header="@Loc["Age"]" />
            </Columns>
        </BUIDataGrid>
    </DocDemo>

    <DocDemo Key="Sortable" Title="@Loc["Sortable"]" Description="@Loc["Enable Sortable on the grid and on the columns you want clickable."]">
        <BUIDataGrid Items="_people" Sortable="true">
            <Columns>
                <BUIDataColumn TItem="Person" Property="@(p => p.Id)" Header="ID" Sortable="true" />
                <BUIDataColumn TItem="Person" Property="@(p => p.Name)" Header="@Loc["Name"]" Sortable="true" />
                <BUIDataColumn TItem="Person" Property="@(p => p.Email)" Header="@Loc["Email"]" />
                <BUIDataColumn TItem="Person" Property="@(p => p.Age)" Header="@Loc["Age"]" Sortable="true" />
            </Columns>
        </BUIDataGrid>
    </DocDemo>

    <DocDemo Key="FilterPage" Title="@Loc["Filter + pagination"]">
        <BUIDataGrid Items="_big"
                     Filterable="true"
                     FilterPlaceholder="@Loc["Search..."]"
                     PageSize="10"
                     ShowPageSizeSelector="true"
                     PageSizeOptions="@(new[] { 5, 10, 25 })">
            <Columns>
                <BUIDataColumn TItem="Person" Property="@(p => p.Id)" Header="ID" Filterable="true" />
                <BUIDataColumn TItem="Person" Property="@(p => p.Name)" Header="@Loc["Name"]" Filterable="true" />
                <BUIDataColumn TItem="Person" Property="@(p => p.Email)" Header="@Loc["Email"]" Filterable="true" />
                <BUIDataColumn TItem="Person" Property="@(p => p.Age)" Header="@Loc["Age"]" Filterable="true" />
            </Columns>
        </BUIDataGrid>
    </DocDemo>

    <DocDemo Key="Selection" Title="@Loc["Multiple selection"]">
            <div>@Loc["Selected"]: <strong>@_selected.Count</strong></div>
            <BUIDataGrid TItem="Person"
                         Items="_people"
                         SelectionMode="SelectionMode.Multiple"
                         SelectedItems="_selected"
                         SelectedItemsChanged="@(s => { _selected = s; StateHasChanged(); })">
                <Columns>
                    <BUIDataColumn TItem="Person" Property="@(p => p.Name)" Header="@Loc["Name"]" />
                    <BUIDataColumn TItem="Person" Property="@(p => p.Email)" Header="@Loc["Email"]" />
                    <BUIDataColumn TItem="Person" Property="@(p => p.Age)" Header="@Loc["Age"]" />
                </Columns>
            </BUIDataGrid>
    </DocDemo>

    <DocDemo Key="Template" Title="@Loc["Custom templates"]" Description="@Loc["Use Template with context to render any markup inside a cell."]">
        <BUIDataGrid Items="_people">
            <Columns>
                <BUIDataColumn TItem="Person" Header="@Loc["Person"]">
                    <Template>
                        <strong>@context.Name</strong>
                        <div style="font-size: 0.85em; opacity: 0.7;">@context.Email</div>
                    </Template>
                </BUIDataColumn>
                <BUIDataColumn TItem="Person" Property="@(p => p.Age)" Header="@Loc["Age"]" Align="ColumnAlign.Right" />
            </Columns>
        </BUIDataGrid>
    </DocDemo>

    <DocDemo Key="Align" Title="@Loc["Alignment & width"]">
        <BUIDataGrid Items="_people">
            <Columns>
                <BUIDataColumn TItem="Person" Property="@(p => p.Name)" Header="@Loc["Name (left)"]" Align="ColumnAlign.Left" Width="200px" />
                <BUIDataColumn TItem="Person" Property="@(p => p.Email)" Header="@Loc["Email (center)"]" Align="ColumnAlign.Center" />
                <BUIDataColumn TItem="Person" Property="@(p => p.Age)" Header="@Loc["Age (right)"]" Align="ColumnAlign.Right" Width="80px" />
            </Columns>
        </BUIDataGrid>
    </DocDemo>

    <DocDemo Key="ShadowAndBorder" Title="@Loc["Shadow & Border"]">
            <BUIDataGrid Items="_people" Shadow="@BUIShadowPresets.Elevation(3)">
                <Columns>
                    <BUIDataColumn TItem="Person" Property="@(p => p.Name)" Header="@Loc["Name"]" />
                    <BUIDataColumn TItem="Person" Property="@(p => p.Age)" Header="@Loc["Age"]" />
                </Columns>
            </BUIDataGrid>
            <BUIDataGrid Items="_people" Border="@BUIBorderPresets.RoundedLarge">
                <Columns>
                    <BUIDataColumn TItem="Person" Property="@(p => p.Name)" Header="@Loc["Name"]" />
                    <BUIDataColumn TItem="Person" Property="@(p => p.Age)" Header="@Loc["Age"]" />
                </Columns>
            </BUIDataGrid>
    </DocDemo>

    <section class="doc-section">
        <h2 class="doc-section__title">@Loc["Properties"]</h2>
        <PropertyTable Properties="@BUIDataGridComponentInfo.Parameters" />
    </section>

</DocPage>

@code {
    private record Person(int Id, string Name, string Email, int Age);

    private readonly List<Person> _people =
    [
        new(1, "Alice Johnson", "alice@example.com", 29),
        new(2, "Marcus Chen", "marcus@example.com", 34),
        new(3, "Priya Patel", "priya@example.com", 41),
        new(4, "Diego Martinez", "diego@example.com", 26),
        new(5, "Hana Nakamura", "hana@example.com", 38)
    ];

    private readonly List<Person> _big = Enumerable.Range(1, 35)
        .Select(i => new Person(i, $"Person {i:D2}", $"person{i}@example.com", 20 + (i % 40)))
        .ToList();

    private HashSet<Person> _selected = new();
}
